"""Loopy coordination tools — the Python library backing every agent action.

Importable as ``tools.*`` once the framework directory (``loopy/``) is on
``sys.path``. The operator scripts and ``scripts/agent-tool.py`` arrange that;
tests rely on the ``pythonpath`` setting in ``pyproject.toml``.
"""

from __future__ import annotations

__version__ = "0.1.0"
